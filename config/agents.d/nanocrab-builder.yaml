agent_id: nanocrab-builder
enabled: true
workspace: ./workspaces/nanocrab-builder

identity:
  name: nanocrab-builder
  emoji: "🛠️"

model_policy:
  primary: sonnet
  fallbacks: [haiku, gpt-4o-mini]

tool_policy:
  allow: [read, write, edit, exec]

memory_policy:
  mode: hippocampus_cortex
  write_scope: conservative

sub_agent:
  allow_spawn: false
