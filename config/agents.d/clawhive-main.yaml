agent_id: clawhive-main
enabled: true
workspace: ./workspaces/clawhive-main

identity:
  name: clawhive
  emoji: "🦀"

model_policy:
  primary: sonnet
  fallbacks: [haiku, gpt-4o-mini]

tool_policy:
  allow: [read, write, edit, exec, web]

memory_policy:
  mode: hippocampus_cortex
  write_scope: conservative
