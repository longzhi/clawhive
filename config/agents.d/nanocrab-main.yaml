agent_id: nanocrab-main
enabled: true
workspace: ./workspaces/nanocrab-main

identity:
  name: nanocrab
  emoji: "🦀"

model_policy:
  primary: sonnet
  fallbacks: [haiku]

tool_policy:
  allow: [read, write, edit, exec, web]

memory_policy:
  mode: hippocampus_cortex
  write_scope: conservative
